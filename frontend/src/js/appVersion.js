// =====================================================================
// src/js/appVersion.js – Zentrale Versionsnummer der PWA
// =====================================================================

(function initMindMirrorVersion(root) {
  root.MIND_MIRROR_APP_VERSION = 'v0.1.0';
})(typeof self !== 'undefined' ? self : window);

// Ende src/js/appVersion.js
